/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

import type { EmailBody } from './EmailBody';

export type RegisterResponseTemplate_In = {
    name: string;
    subject?: (string | null);
    body?: (EmailBody | null);
    note?: (string | null);
};
